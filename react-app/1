import React from "react";

const VideoEditForm = ({ closeModal, video }) => {
  return (
    <h1
      style={{ color: "red", fontSize: 30, margin: "auto", gridArea: "form" }}
    >
      This feature is in progress, check back later!
    </h1>
  );
};

export default VideoEditForm;
